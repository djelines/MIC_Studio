# ============================================================
# .env.example — valeurs vides ou fictives, à commiter.
# Copier en .env (jamais commité) et remplir les vraies valeurs.
# ============================================================

# Actuellement ce projet est un SPA pur sans backend.
# Ces variables sont prévues pour une future API backend.

# Clé secrète JWT — générer avec : openssl rand -hex 32
# Prévu pour une future API backend, pas utilisé actuellement.
JWT_SECRET=
